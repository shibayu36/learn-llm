[project]
name = "learn-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shibayu36", email = "shibayu36@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "tiktoken>=0.14.0",
]

[project.scripts]
learn-llm = "learn_llm:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
