# ハンズオン資料の進捗

全体の方針・構成・実験条件は `docs/handson-plan.md` を参照する。ここでは作成と確認の状態を管理する。チェックは、その工程が完了したときに付ける。ドラフトの作成と、ユーザーによる方向性の確認を分ける。

## 現在の作業

Part 1のMarkdown本文、`sample/` の参照コード、操作デモを作成した。本文とコードの整合性を確認し、ユーザーのフィードバックを反映している。本文はMarkdown、操作デモは `docs/demos/part1.html` の1ファイルとし、本文からフラグメントでリンクする。静的な図はMarkdownに置く。

日本語版をPart 1の標準へ統合した。固定したFineWeb-2 Edu Japaneseの1万文書と、trainの文字だけから作った語彙4,052の文字単位Tokenizerを使う。2026-09-24にBPEから文字単位へ変更した。本文・参照コード・生成例・loss curve・HTMLの生成再生を日本語へ揃えた。取得条件とハッシュは `sample/dataset-manifest.json`、完成コードは `sample/tiny_gpt/` に置く。

## 日本語版への更新

- [x] 1万文書を選び、train 9,000文書・validation 1,000文書を固定する
- [x] 取得元のrevision・選択条件・SHA-256を記録する
- [x] 再取得した内容の一致と、train/validation間のID・本文の重複がないことを確認する
- [x] 正常終了する取得方式にそろえる
- [x] trainの文字だけから語彙を作る文字単位Tokenizerを用意する
- [x] 日本語版を `sample/tiny_gpt/` に統合し、本文の完成コードとそろえる
- [x] 語彙作成とGPTの学習の違い、EOS、UNKを説明する
- [x] 学習前後の日本語生成例・loss curveを本文へ反映する
- [x] 10,000回の学習・評価・生成・保存を確認する
- [x] 型注釈・本文とコードの一致・リンクを自動確認する
- [ ] 日本語版の本文とデモをユーザーが確認する

日本語版の11件の自動テストが通過。文字単位Tokenizerで10,000回の学習・定期評価をApple M5 Proで実行し、約69秒。validation lossは8.5111から3.5536へ低下した。日本語の語句や文末のつながりが現れたが、入力の話題を保った説明にはなっていない。実測結果は `sample/results/README.md` にまとめた。

データ取得は、保存後にstreamingの終了処理が停止したため、ユーザーの承認を受けて最初のParquetファイル1つを取得する方式へ変更した。正常終了と保存済みデータとのハッシュ一致を確認済み。

## 視覚表現の更新

- [x] 何を理解させるかが曖昧だった、Q/K/Vの数値を直接編集するデモを置き換える
- [x] 参考HTMLから内積の矢印・softmaxへの変換・重み付き和・Wの編集の見せ方を取り入れる
- [x] Causal Maskで「次に当てるtokenが入力の先にある」状態を示す
- [x] greedy・sampling・temperatureのデモを日本語の候補へそろえる
- [x] 日本語モデルの実際の確率と選択を記録し、1tokenずつ再生する
- [x] 各デモの学習目的を計画に記録し、本文の対応箇所からリンクする
- [x] ブラウザで初期表示・内積からsoftmaxへの変換・重み付き和・Wの変更・Causal Mask・greedy/samplingを確認する
- [x] 日本語生成の前後12ステップ・停止状態・表示と記録の一致を自動確認する

本文の確認コードをそのまま実行し、日本語の入力・正解が各10token、モデル出力が `[1, 11, 4052]` になることも確認した。ブラウザでの生成デモ確認は初期表示までとし、再生の全ステップは表示関数の自動チェックで確認した。

## 全体のTODO

- [x] Part 2以降の制作基準をユーザーと合意し、計画の「制作基準」へ記録する
- [ ] Markdownで1章の方向性を固める
- [x] 1章に必要な動く視覚表現を別HTMLの参考資料として作り、動作確認する
- [ ] ユーザーが1章の本文と参考HTMLを確認する
- [ ] ユーザーが1章を実践する（1.2まで実践中。実験より先に、いま進めている節のコードと本文を直す）
- [ ] 実践で見つかった1章の改善点を反映する
  - [x] CPU固定にすると決定し、`device`・`select_device`・`synchronize` をコードと本文から削除（2026-09-24）
  - [x] 「理解する単位で載せる」基準（handson-plan.md 制作基準）に従い、先回りしていたコードを使う節へ移動：`load_data`・`make_batch` を1.2→1.11、`Tokenizer.save` を1.2→1.13、`Tokenizer.load` は削除、1.11の保存系importを1.13へ。1.3〜1.5・1.8・1.9・1.11・1.12に、その節の部品を実際に動かす動作確認・実験を追加
  - [x] CPU固定で `results/` を取り直した。loss・生成文はMPSの結果と一致し、時間だけ約69秒→約171秒に変わった。本文・README・計画の時間を更新済み。デモHTMLは旧教材のトレースを埋め込んでいるため影響なし
  - [x] スレッド数はPyTorch既定（性能コア5本）が最速だったので設定しない
  - [x] 1.3・1.4が `main.py` で `nn.Embedding` を試すだけで `model.py` に何も残らなかったのを、1.3で `TinyGPT` の骨格（Token Embeddingのみ）を作り、1.4でPosition Embeddingを足し、1.10で残りの部品を足して完成させる形へ変更（2026-09-24）。1.5〜1.9の追加位置を「`TinyGPT` の前」と明記し、`math`・`F` のimportは使う節へ移動。1.1の実行できない断片は「実装」見出しを外して本文に含めた。本文どおり組み立てた `model.py` が `sample/tiny_gpt/model.py` と一致することを `tmp/assemble_model.py` で確認
  - [x] `model.py` を「1.3で完成時の構造を空実装で先に置き、各節で中身を埋める」形へ変更（2026-09-24）。`TransformerBlock`・`LayerNorm`・`FeedForward` は入力をそのまま返す空実装、Attention weightも返す `scaled_attention`・`SelfAttention` は `NotImplementedError` の空実装。`TinyGPT` の `__init__`・`forward` は1.3から完成形で、自作クラスでない `lm_head` だけ `nn.Identity` にして1.10で `nn.Linear` へ置き換える。1.5〜1.9の指示は「`TinyGPT` の前に追加」から「空実装を置き換え」に変更。制作基準に `model.py` だけの例外として記録。組み立て確認は `tmp/assemble_model.py` を置き換え方式へ更新
  - [x] `B` の説明を補強（2026-09-24）。1.3で `B` が何に対応するか分からず混乱したため、導入の記号表の直後に「`B` はモデルの構造と無関係で、系列同士は混ざらない、1.11まで常に1」を追加し、記号表の例を「学習時16、動作確認では1」に変更。1.3の式の段落を「まとめて受け取れるようにしておくので `[B, T]` と書く」に直し、動作確認で `[[2, 5, 2]]` の外側の括弧がバッチの軸で `B=1` だと明記
  - [x] `encode_documents` を1.2から1.11へ移動（2026-09-24）。1.2で全文書をつなぐ理由が分からなかったため、`dataset.py` は1.11で `make_batch` と一緒に新規作成し、つなぐ理由を窓の切り出しと続けて説明する。EOSのつなぎ目の動作確認も1.11へ移動
  - [x] 1.3の動作確認を、語彙6の玩具モデル（猫犬猫）から1.2のTokenizerで「日本の」を通す形へ変更（2026-09-24）。本文の例（ID 1879・1959・482、語彙4,052行）と実行結果が直結し、`main.py` は1.2の内容を残して末尾に追記する。最後に `x[0]` が `token_embedding.weight[ids[0]]` と一致することを `torch.equal` で確認。1.4も追従し、1.3の `main.py` をそのまま実行して比較が `False` に変わるのを見てから、末尾に位置の表の行を足した和と `x[0]` が一致する確認を追記する形にした。`main.py` は節ごとに書き換えるのではなく、1.2→1.3→1.4と下に追記していく
  - [x] Part 1全節を「概念を理解させる文章」へ書き直し（2026-09-24）。「実装内容を記述しているだけで理解させる文章になっていない」との指摘を受け、全節を「抽象（太字の一言）→ 具体（例文をテキスト図で追う）→ 意味 → 一般化（式・shape）→ 実装」の型に統一。shape表・添字式を先頭から外し、1.5のQ・K・V、1.7のMLP（両方そろったときだけ反応する特徴の数値例と手置き重みの実験）、1.8のResidual・LayerNorm、1.10のLM Head（内積としての意味と学習後の実測logit）、1.11の勾配（logitに対する勾配の向きの実験）に意味の説明を追加。`model.py`・`train.py`・`dataset.py`・`config.py` のコメントも「何を作るか・結果が何を表すか・なぜか」に書き換え、本文と `sample/` を一致させた。書き直し後に全文を再監査し、数値の矛盾・用語の揺れ（点数とlogit）・スコアとAttention weightの混同などを修正
- [ ] 2章を作る
- [ ] ユーザーが2章を確認・実践する
- [ ] 3章を作る
- [ ] ユーザーが3章を確認・実践する
- [ ] 4章を作る
- [ ] ユーザーが4章を確認・実践する
- [ ] 5章を作る
- [ ] ユーザーが5章を確認・実践する
- [ ] 付録と全体の整合性を確認する

## 1章：Markdown

- [x] 読者・目的・実験時間・データの言語を整理する
- [x] 参考の文章・HTML・サンプルコードの方針を確認する
- [x] 全体の章構成と設計案を提示する
- [x] 1.1〜1.13のドラフトを作る
- [x] アーキテクチャ図を冒頭に置き、操作デモへリンクする
- [x] `sample/` に参照コードを配置する
- [x] 読者の別の作業場所での実装手順を明記する
- [x] コードの動作と本文との一致を確認する
- [x] 2つの文章スキルに沿って推敲する
- [x] Pythonの型注釈を本文と参照コードへ付ける
- [x] 未学習GPTの生成を冒頭で試せるようにする
- [x] 書きかけの日本語文を入力し、学習前後で同じ続きを生成する
- [x] 実行デバイスをautoにし、本文から選択の案内を外す
- [ ] ユーザーが説明の順序・粒度・コードの見せ方を確認する
- [ ] フィードバックを反映し、Markdownの方向性を確定する

## 1章：参考HTMLと実践

- [x] 理解を助ける動く図・操作デモを選ぶ
- [x] `docs/demos/` に参考HTMLを作り、本文から案内する
- [x] 参考HTMLにコードを載せる場合はDark+で表示する
- [x] 実測のloss curve・生成例・実験条件を掲載する
- [x] ブラウザで表示と操作を確認する
- [ ] ユーザーが参考HTMLの見せ方を確認する
- [ ] ユーザーが参照コードを見ながら別の場所に実装する
- [ ] 所要時間・分かりにくい説明・詰まった手順を記録する
- [ ] 実践で見つかった改善点を反映する

## 2章以降の共通工程

`docs/handson-plan.md` の「制作基準」を適用する。各章で、構成と変更点の提示→本文と参照コードの作成→動作確認と比較実験→説明・結果の確認→ユーザーの実践、の順に進める。複数の章を同時に作り進めない。

## 確認待ち・実践メモ

- 参照コードは `sample/`、ユーザーの実装は別の作業場所とする。
- 実行の入口は作業用ディレクトリ直下の `main.py` に一本化する（2026-09-24）。`tiny_gpt/` の部品ファイルには `main()` や `if __name__` を置かず、各節の確認コードで `main.py` を書き換えて実行する。`check.py` と `python -m tiny_gpt.xxx` は使わない。
- 制作事情や過去の方針を本文へ混ぜず、ここで管理する。所要時間は実行条件と実測を踏まえて本文へ示す。
- Python・主要ライブラリは最新安定版へ揃える。採用版は計画資料に記録する。
- `core-message-writing` によるサブエージェントレビューと認知しづらい箇所のレビューを完了し、妥当な指摘を反映した。
- `cursor-agent --model gemini-3.7-flash-high` の日本語レビューを、ユーザーが承認した本文だけの一時ディレクトリで実行し、妥当な指摘を反映した。
- 入力例は「日本の／首都／は／東京／です／。」とし、shapeの `B` と区別する。語彙数は `vocab_size` と書き、Valueの `V` と区別する。
- 冒頭にBlock内部まで含むアーキテクチャ図を配置した。実験は節ごとに必須にせず、理解に役立つ比較と全体の動作確認へ絞った。
- 確認済みの実行条件と結果は、このページの「日本語版への更新」に記録する。
